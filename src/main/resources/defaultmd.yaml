# standard metadata constraints
constraints:
  - constraint:
    name: Source-Organization
    required: false
    repeat: true
    values: []
  - constraint:
    name: Organization-Address
    required: false
    repeat: true
    values: []